NODE_ENV=development
PORT=5000
CLIENT_URL=http://localhost:5173

DATABASE_URL=postgres://postgres:Surajyadav123@127.0.0.1:5432/traveloop
# Set to true only when you want to run the API without PostgreSQL.
SKIP_DB=false

JWT_SECRET=replace_this_with_a_long_random_secret
JWT_EXPIRES_IN=7d

